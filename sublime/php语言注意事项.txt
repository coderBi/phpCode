1, php里面两个int 进行运算的结果可能是float
2，php里面任何地方用了die($str) 或者 exit（$str） 或者在非
	函数内部直接(在php标签里面)调用 return 都会导致相应语句
	下面的代码不在进行执行，这里不执行的代码不仅包括php本身
	代码，也包括后面的所有html标签（例如 body html）都不会
	再进行显示了。其原因是php读取相应的php文档到了exit 或者
	die语句就会停止解析
3, php里面的sleep参数 表示的单位是 “秒”
4，php文件里面的return可以用来终止文件的载入。当然也可以用于
	返回载入文件的结果
5,php里面函数定义先后顺序不影响相互调用，也就是前面的可以调用后面的（跟c c++不同 跟java一致）